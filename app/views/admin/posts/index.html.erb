<div id="page-info" class="in">
  <h2>Blog Posts</h2>
  <p>This is a list of all the blog's posts. From here you can view, modify, or delete any post on the blog.</p>
</div>
<div class="line"></div>
<div id="page-actions" class="in">
  <%= link_to "New Blog Post", new_admin_post_path %>
  <%= form_tag admin_posts_path, :method => :get, :class => "object-search" do %>
    <%= hidden_field_tag :sort, params[:sort] %>
    <%= hidden_field_tag :order, params[:order]%>
    <div>
      <%= text_field_tag :search, params[:search] %><%= submit_tag "Search", :name => nil %>
    </div>
  <% end %>
</div>
<div id="posts" class="in">
  <%= render "posts" %>
</div>
