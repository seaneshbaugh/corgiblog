<h2><%= t("posts.#{@post.persisted? ? 'edit' : 'new'}.title") %></h2>
<%= simple_form_for [:admin, @post], :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= t('posts.sections.information') %></legend>
    <%= f.input :title %>
    <%= f.input :body %>
    <%= f.input :style %>
    <%= f.input :visible %>
    <%= f.input :user_id, :as => :hidden, :input_html => { :value => current_user.id } %>
  </fieldset>
  <div class="form-actions">
    <%= link_to t('posts.links.back_to_index'), admin_posts_path, :class => 'btn' %>
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <% unless @post.persisted? %>
      <label class="checkbox inline">
        <%= check_box_tag 'redirect_to_new', 1, true %> <%= t('helpers.submit.post.redirect_to_new') %>
      </label>
    <% end %>
  </div>
<% end %>
